1. ~~書架首頁要有分頁，24本1頁，頁尾要有回到頁首的按鈕~~
2. ~~書架首頁排序以檔案建立新->舊排列~~
3. ~~資料庫的欄位要有檔案建立日期的欄位~~
4. ~~要能自己添加書籍資料，除了用掃描條碼的方式外，也要能手動輸入ISBN查詢書籍資料來新增~~
5. ~~新增書籍資料的封面圖，要能下載回本地端再上傳到伺服器，目前封面圖會直接連結網路圖片，若是網路圖片掛掉就無法顯示~~
6. ~~將查詢機制改為「優先直接使用三民站內搜尋」，目前依賴 DuckDuckGo 外部搜尋引擎容易受到搜尋排名與海外電商干擾~~
7. ~~資料庫新增 UUID/UID 欄位設計（Book、MyBook、Shelf 表），供手機端離線新增書籍時產生唯一識別碼，以支援後續回區網時的「手動按鈕雙向無衝突同步」~~
8. ~~拿掉 閱讀中 未讀 已讀 這類閱讀狀態相關標籤，個人評分也不需要，首頁上方保留「所有書籍」以及「所屬書架」的篩選，下方以卡片方式列出書籍，卡片要有書籍名稱、作者、書封圖片~~
9. ~~加入時間的貌似沒有使用+8的時區，請修正~~
10. ~~已有的書封圖片保留未來替換的功能~~
11. ~~需要增加匯出成csv的功能，連同用到的書封圖片，一起打包成zip~~
12. ~~當刪除收藏（移除書籍）時，要順道把本地儲存的對應書封圖片實體檔案一併刪除，避免儲存過多沒用到的圖片佔用空間。~~
13. ~~方案 A：手動同步時在背景全量預載所有藏書封面至離線快取（Prefetch All Covers），解決手機離線/開飛航模式時翻到第 2 頁以後看不到書封圖片的問題~~
14. ~~PWA 離線升級為「快取優先 (Cache-First)」與版號本地持久化記憶~~
    - 修改 `app/static/sw.js`：靜態資源全面改為 Cache First，解決出門在 4G/5G 下敲 192.168 區網 IP 造成 30~60 秒連線逾時卡死/白畫面問題。
    - 修改 `app/static/js/app.js`：新增 `fetchSwFileVersion()` 降級解析備援，並以 `localStorage` 持久化儲存版號；即使在純 HTTP 區網或完全離線環境，版號亦永不消失。
    - 支援帶小數點版號縮寫（如 `v4.1`、`v4.2`），並移除同步彈窗中歷史寫死的 `v5` 預設字串。
15. ~~相機掃描條碼模組修復與非 HTTPS 安全提示~~
    - 修復類別命名不一致：`scanner.js` 定義 `ISBNScanner` 但 `app.js` 呼叫 `BarcodeScanner` 造成執行階段未定義報錯。已於雙邊建立相容別名並加入防禦性例外捕獲。
    - 加入安全上下文檢查提示：若於純 HTTP 區網下，瀏覽器禁止取用 `getUserMedia` 鏡頭串流時，主動跳出明確提示引導使用者改用右下角「✍️ 手動輸入 ISBN」。
16. **【進行中 / 待驗證】測試手機離開區網的使用狀況**：
    - [ ] NAS 重新 build 容器：`cd /volume2/docker/book-storge && sudo docker compose up -d --build`
    - [ ] 手機在區網內開啟 App 下拉重新整理，確認右上角版號成功更新為最新版本（如 `v4.2`）。
    - [ ] 關閉手機 Wi-Fi（切換至 4G/5G 行動網路或開啟飛航模式），滑掉 App 重新點開。
    - [ ] 驗證離開區網時是否能 0 秒秒開藏書清單、全文檢索、書架切換與書封瀏覽，確認不再出現白畫面逾時。
    - [ ] 設定NAS反向代理，讓app取得鏡頭啟用權，只要通過一次 HTTPS 驗證，走到天涯海角、離線斷網，鏡頭都能隨時打開掃碼。

---
cd /volume2/docker/book-storge
sudo docker compose up -d --build


